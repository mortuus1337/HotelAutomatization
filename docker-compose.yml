services:
  postgres:
    image: postgres:16
    container_name: postgresBD
    environment:
      POSTGRES_DB: hoteldb
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
    ports:
      - "5432:5432"
    volumes:
      - hotel_pg_data:/var/lib/postgresql/data

volumes:
  hotel_pg_data: